# Spiking neuromorphic SLAM (2D) — pinned environment (Python 3.14)
# Reproduces every figure and reported number in the paper.
jax==0.10.2
jaxlib==0.10.2
flax==0.12.7
optax==0.2.8
numpy==2.5.0
scipy==1.18.0
matplotlib==3.11.0
msgpack>=1.0.0          # frozen CSNN weights (src/frozen_csnn_weights.msgpack)
cairosvg>=2.7.0         # Fig 1: render the SVG vector master -> PDF+PNG (or use rsvg-convert/inkscape)
